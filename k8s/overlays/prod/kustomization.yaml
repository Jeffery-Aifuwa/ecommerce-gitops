apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: store-prod

resources:
  - ../../base

namePrefix: prod-

patches:
  - target:
      kind: Deployment
      name: store-ui
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
