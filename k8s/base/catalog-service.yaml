apiVersion: v1
kind: Service
metadata:
  name: catalog-api-svc
  labels:
    app: catalog-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 5678
  selector:
    app: catalog-api
